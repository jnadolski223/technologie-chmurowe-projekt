apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-headless
  namespace: event-manager-app
  labels:
    app: rabbitmq
spec:
  clusterIP: None
  selector:
    app: rabbitmq
  ports:
    - port: 5672
      name: rabbitmq